# CRM 阶段 2 全员分支审查与整改清单

> 审查日期：2026-07-16  
> 唯一实现依据：`docs/01-aisdd/CRM阶段2统一开发基线.md` V1.5  
> 代码比较基点：`origin/develop`，提交 `a497016`  
> 审查范围：本地已同步的 `origin/feature/*` 远程跟踪分支  
> 结论用途：成员整改、组长复核和后续合并；本文不替代 V1.5

## 1. 总体结论

截至本次审查，没有成员分支达到合入 `develop` 的全部门槛。

1. `feature/workorder`、`feature/marketing`、`feature/opportunity` 已有阶段 2 提交，但存在阻塞问题，暂不得合并。
2. `feature/oa` 的 D2-CON-01 已形成独立提交并推送，改动范围干净；完整测试和合并仍需等待 D2-QA-01 测试底座通过。
3. `feature/customer`、`feature/order`、`feature/public-integration` 尚未发现相对 `origin/develop` 的阶段 2 提交。
4. `feature/finance` 未发现阶段 2 提交；唐文军当前请假，属于计划内保留任务，不记为异常。
5. V1.5 规定必须先合入 D2-QA-01。在测试底座修正并连续运行两次通过前，其他任务只能继续在各自功能分支整改或开发，不得合入 `develop`。

## 2. 当前分支快照

| 成员 | 任务 | 分支 | 当前提交 | 审查状态 |
|---|---|---|---|---|
| 王文渊 | D2-CON-01 | `feature/oa` | `0d1bc6e` | 代码已提交；等待 QA 前置和完整验证 |
| 曾皓冲 | D2-MKT-01 | `feature/marketing` | `8b68940` | 有阻塞问题，退回整改 |
| 姚昱竹 | D2-STAT-01 | `feature/public-integration` | `a497016` | 尚无阶段 2 提交 |
| 李祖豪 | D2-CUS-02、D2-CUS-03 | `feature/order` | `a497016` | 尚无阶段 2 提交 |
| 安炳琨 | D2-QA-01 | `feature/workorder` | `9fd443e` | 有阻塞问题，且阻塞全组后续合并 |
| 刘焘玮 | D2-CUS-01 | `feature/customer` | `a497016` | 尚无阶段 2 提交 |
| 唐文军 | D2-REC-01 | `feature/finance` | `a497016` | 请假，任务保留，不排程 |
| 黄金戈 | D2-BIZ-01 | `feature/opportunity` | `6f992b4` | 有多项阻塞问题，退回整改 |

## 3. 各成员问题与整改要求

### 3.1 安炳琨：D2-QA-01 / `feature/workorder`

当前改动为 5 个新增文件、约 1278 行，文件范围基本符合测试底座任务，但实现尚未满足 V1.5 验收。

#### P0：测试数据工厂没有真正实现租户隔离

**已核验问题：**

- `CrmTestDataFactory` 的多个创建方法接收 `tenantId`，但没有把它写入对应多租户 DO。
- `getCustomerCount(Long tenantId)`、`getClueCount(Long tenantId)` 等计数方法没有按 `tenantId` 过滤，而是统计全表。
- `testDataIsolation()` 只断言两个对象 ID 不相同。ID 不同不能证明租户隔离，也不能证明跨租户查询被阻止。

**必须修改：**

1. 对所有继承 `TenantBaseDO` 的测试对象显式设置传入的 `tenantId`。
2. 所有带 `tenantId` 的查询或计数方法必须按租户过滤。
3. 租户隔离测试必须断言：租户 A 只能查询租户 A 数据、租户 B 只能查询租户 B 数据，并覆盖跨租户不可见。
4. 不得使用“ID 不同”替代租户隔离断言。

#### P0：缺少 V1.5 要求的空值测试

**已核验问题：**`CrmTestSupportTest` 有正常创建、批量创建、清理等测试，但没有测试数据工厂自身的空值断言。

**必须修改：**补充空值/非法输入场景，并明确期望结果；不能只断言对象非空。

#### P0：运行说明错误依赖真实 Redis

**已核验问题：**`test-run-guide.md` 要求真实 Redis 端口，而 V1.5 明确禁止真实 Redis、外部数据库和网络依赖。

**必须修改：**

1. 删除真实 Redis 启动前置说明。
2. 统一使用既有 `BaseDbUnitTest`；确需 Redis 能力时使用既有 `BaseDbAndRedisUnitTest` 的测试替身，不连接真实服务。

#### 复核门槛

- 在干净环境运行 CRM 模块测试连续两次。
- 两次都必须成功加载上下文，且不得出现表已存在、脏数据、真实 Redis、外部数据库或网络依赖失败。
- 提交两次完整命令、退出码和测试汇总；不能只提交说明文档或截图结论。

### 3.2 曾皓冲：D2-MKT-01 / `feature/marketing`

当前改动为 5 个文件、约 109 行，任务主体方向正确，但有 SQL 和文件范围问题。

#### P0：回滚 SQL 使用了不可直接执行的顶层 `IF`

**已核验问题：**`20260716_d2_business_lose_reason_dict_rollback.sql` 在普通 MySQL 脚本顶层使用 `IF ... THEN ... END IF`。该语法只能在存储程序等复合语句环境中使用，当前脚本不能按普通迁移脚本直接执行。

**必须修改：**使用项目可直接执行的 MySQL 语句实现引用计数与条件删除；脚本发现引用时必须停止回滚并报告引用数量，不能删除已有字典含义。

#### P0：迁移文件位置不符合项目增量 SQL 约定

**已核验问题：**正向和回滚脚本放在 `Server/sql/mysql`，而项目开发计划规定增量 SQL 放入 `database/new`；新增 SQL 还必须纳入初始化执行顺序。

**必须修改：**将两份脚本放入 `database/new`，文件名保持 V1.5 固定名称，并在变更说明中写明执行顺序、初始化接入方式和回滚方法。

#### P1：包含与任务无关的通用字典工具改写

**已核验问题：**`Web/src/utils/dict.ts` 除新增 `CRM_BUSINESS_LOSE_REASON` 外，还把 `getDictLabel` 的 `ref` 实现改为普通变量。该变化不是 D2-MKT-01 所需，会扩大共享工具风险。

**必须修改：**保留字典常量新增，撤销 `getDictLabel` 的无关重构。

#### P1：所谓“前后端常量一致”测试没有验证前端

**已核验问题：**`testCrmBusinessLoseReasonMatchesFrontendConstant()` 只是再次将后端常量与同一个字符串比较，没有读取或验证前端 `DICT_TYPE`。

**必须修改：**至少提供可追溯的前后端一致性核验；测试名称不得宣称验证了实际上没有验证的对象。

#### 复核门槛

- 正向 SQL 在空库和已执行一次的数据库上均可运行，证明幂等。
- 回滚 SQL 在“无引用”时成功删除，在“有引用”时明确阻止删除并报告数量。
- 分支最终只保留 V1.5 分配的字典常量、迁移/回滚 SQL、任务测试和说明。

### 3.3 黄金戈：D2-BIZ-01 / `feature/opportunity`

当前一次提交修改 30 个文件，约 1815 行新增、362 行删除。改动规模较大，并与 D2-MKT-01、共享字典常量及未登记接口发生冲突，暂不得进入合并。

#### P0：固定错误码不符合 V1.5

**已核验问题：**V1.5 固定要求 `BUSINESS_UPDATE_VERSION_CONFLICT=1_020_002_004`，当前代码使用 `BUSINESS_VERSION_CONFLICT=1_020_002_008`，并把 `1_020_002_004` 分配给其他错误。

**必须修改：**严格采用 V1.5 的错误码名称、数值和提示，不得复用或另行分配。

#### P0：迁移文件名错误且缺少独立回滚 SQL

**已核验问题：**当前仅有 `database/new/crm-business-opportunity-upgrade.sql`，文件末尾只有回滚说明，没有 V1.5 固定的独立回滚脚本。

**必须修改：**

1. 正向文件改为 `20260716_d2_business_state_machine.sql`。
2. 新增 `20260716_d2_business_state_machine_rollback.sql`。
3. 回滚按 V1.5 删除本任务新增字段，并提供实际执行验证记录。
4. 不得用注释中的“人工回滚说明”替代可执行回滚脚本。

#### P0：未按固定字典校验边界实现

**已核验问题：**服务代码自行调用 `getDictDataList` 后遍历校验；V1.5 明确要求调用既有 `DictDataApi.validateDictDataList("crm_business_lose_reason", ...)`。

**必须修改：**改用 V1.5 指定公开边界，不得自建一套平行校验逻辑。

#### P0：新增基线未登记接口

**已核验问题：**分支新增 `PUT /crm/business/update-quotation` 及配套 VO/API。V1.5 没有登记该新接口，并要求商机状态只使用 `PUT /crm/business/update-status`，报价和产品更新应复用既有公开边界。

**必须修改：**删除新增平行接口，复用 V1.5 登记的既有商机/产品更新接口；若确实无法实现，提交“基线补充申请”，在基线更新前不得保留该接口。

#### P0：越过 D2-MKT-01 的文件所有权

**已核验问题：**商机分支修改 `DictTypeConstants`，但输单原因字典常量属于 `feature/marketing` 的 D2-MKT-01。两个分支同时修改会产生所有权冲突和重复实现。

**必须修改：**撤销商机分支中的字典常量定义，等待 D2-MKT-01 合入后同步最新 `origin/develop`，再调用该常量。

#### P1：迁移包含超出固定契约的数据库变化

**已核验问题：**迁移除新增 `version`、`lose_reason_code` 外，还修改 `crm_business_status.percent` 类型并进行额外阶段回填；这些变化没有对应的独立裁决和回滚。

**必须修改：**仅保留 V1.5 明确规定的字段与历史数据归一逻辑。其他数据库变化必须删除，或先提交基线补充申请并等待统一基线更新。

#### 复核门槛

- 先等待修正后的 D2-MKT-01 合入，再同步最新 `origin/develop`。
- 重新提交精简后的文件清单，逐项对应 V1.5 D2-BIZ-01。
- 自动化测试覆盖双填、空填、禁止回退、终态保护、输单原因、版本冲突、租户条件更新及金额/产品事务回滚。
- 未修正 D2-QA-01 前，可以编写测试，但不得宣称测试已完整通过。

### 3.4 王文渊：D2-CON-01 / `feature/oa`

**已核验状态：**提交 `0d1bc6e` 已推送到 `feature/oa`，只包含合同 API 类型、合同页面、合同待办列表和联调记录，未发现跨任务文件。

**当前阻塞：**不是代码退回问题，而是全组测试前置尚未满足。D2-QA-01 未通过前，不能完成统一测试环境下的完整接口验证，也不能合入 `develop`。

**下一步：**

1. 保持现有提交，不重复重写已符合 V1.5 的代码。
2. D2-QA-01 合入后同步 `origin/develop`，执行合同提交、待办跳转、无流程实例保护、权限和五态显示测试。
3. 由黄金戈完成 CT-01 交叉测试并提交证据后，再申请合并。

### 3.5 刘焘玮：D2-CUS-01 / `feature/customer`

**已核验状态：**当前远程跟踪分支与 `origin/develop` 同为 `a497016`，尚无阶段 2 可审查提交。

**必须完成：**按 V1.5 实现客户查重接口、DTO、服务、Mapper、必要前端调用、权限及任务测试；不得修改 D2-CUS-02/03 的公海历史和领取限制文件。

**提交前自检：**手机号标准化、名称 Levenshtein 相似度 0.80 边界、`excludeId`、手机号脱敏、无候选空数组、权限和跨租户隔离。

### 3.6 李祖豪：D2-CUS-02、D2-CUS-03 / `feature/order`

**已核验状态：**当前远程跟踪分支与 `origin/develop` 同为 `a497016`，尚无阶段 2 可审查提交。

**必须按顺序实施：**先完成 D2-CUS-02 的两类历史表、迁移/回滚、查询和历史写入；通过后再实施依赖它的 D2-CUS-03 领取限制、冷却期和并发保护。不得把两项压成一个无法审查的大提交。

**提交前自检：**固定接口、固定错误码、租户唯一配置、条件更新防并发、失败事务回滚、正向/回滚 SQL 及初始化顺序。

### 3.7 姚昱竹：D2-STAT-01 / `feature/public-integration`

**已核验状态：**当前远程跟踪分支与 `origin/develop` 同为 `a497016`，尚无阶段 2 可审查提交。

**必须完成：**只新增 `GET /crm/statistics-customer/export-contract-summary`，复用既有 `getContractSummary` 服务和数据权限；补齐导出按钮、唯一权限、正向/回滚 SQL 和测试。不得新建平行统计 Controller、统计表或重复查询接口。

**提交前自检：**空值导出为空单元格、行业/来源字典名称、空数据、日期边界、部门/负责人数据范围、无权限和跨租户隔离。

### 3.8 唐文军：D2-REC-01 / `feature/finance`

**已核验状态：**当前分支与 `origin/develop` 同为 `a497016`。唐文军请假，V1.5 和任务分工计划均要求任务保留、请假期间不开发、不测试、不参与 Git 合并。

**处理结论：**当前没有整改要求，也不得安排他人代写财务域。返岗后重新同步最新 `origin/develop`，只在 V1.5 明确的 D2-REC-01 范围内开发；基线未登记的回款字段、接口、状态或规则必须先提交基线补充申请。

## 4. 统一整改与重新提交要求

每位成员整改或首次提交时，必须同时给出：

1. 任务编号、成员、分支和最新提交号。
2. 实际修改文件清单，并说明每个文件对应哪条 V1.5 要求。
3. 接口契约；没有新增接口时也要明确写“无新增接口”。
4. SQL 正向执行、重复执行和回滚结果；不涉及数据库时写明“不涉及”。
5. 测试命令、退出码、通过/失败数量和完整失败原因。
6. 正常、边界、权限、租户隔离、并发和事务回滚的覆盖证据；不适用项要说明原因，不能直接省略。
7. 未验证风险和依赖阻塞，不得把“代码已写完”写成“验收通过”。

## 5. 组长复核与合并顺序

1. 安炳琨修正 D2-QA-01，并在干净环境连续运行两次通过。
2. 组长复核并合入 `feature/workorder`。
3. `feature/customer`、`feature/public-integration`、修正后的 `feature/marketing` 可分别进入评审。
4. D2-MKT-01 合入后，黄金戈同步最新 `origin/develop`，再提交精简并修正后的 D2-BIZ-01。
5. 李祖豪先提交 D2-CUS-02，再提交 D2-CUS-03。
6. 王文渊在测试底座可用后补齐 D2-CON-01 完整测试和 CT-01 交叉测试证据。
7. 每个分支只有在代码审查、自动化测试、交叉测试、SQL/回滚验证和依赖前置全部通过后，才允许合入 `develop`。

## 6. 成员整改回复模板

```text
成员：
任务：
分支：
最新提交：

已修复问题：
1.
2.

实际修改文件：
-

接口变化：
-

SQL 正向/重复执行/回滚结果：
-

测试命令与退出码：
-

测试结果：
- 正常：
- 边界：
- 权限：
- 租户隔离：
- 并发：
- 事务回滚：

尚未验证的风险或依赖：
-
```

